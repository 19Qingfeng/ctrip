import * as React from 'react';
import { styled } from '@mui/material/styles';

const <%= componentName %>Styled = styled('div')(({ theme }) => ({
  width: 300,
  color: theme.palette.primary.main
}));

export default <%= componentName %>Styled
